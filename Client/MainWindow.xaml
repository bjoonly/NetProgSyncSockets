<Window x:Class="Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Client"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition></RowDefinition>

        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition Width="Auto"></ColumnDefinition>

        </Grid.ColumnDefinitions>
        <TextBlock FontSize="15" VerticalAlignment="Center" Margin="5">Postcode:</TextBlock>
        <TextBox x:Name="textBox" Grid.Column="1" FontSize="15" Height="40"></TextBox>
        <Button Grid.Column="2" Margin="5,0" Width="200" Click="Button_Click">Search</Button>
        
        
        <ListBox x:Name="listBox" Grid.Row="2" Grid.ColumnSpan="3" Margin="5"></ListBox>
    </Grid>
</Window>
